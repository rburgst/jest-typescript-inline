## Sample project showing a jest bug

1. install dependencies 
    ```
    yarn install
    ```

2. run the tests

    ```
    npx jest
    ```

3. update the snapshot using

    ```
    npx jest -u 
    ```
